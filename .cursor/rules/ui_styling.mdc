---
alwaysApply: true
---
# UI & Styling

## Responsive Design
```dart
// ✅ GOOD: LayoutBuilder 사용
LayoutBuilder(
  builder: (context, constraints) {
    if (constraints.maxWidth > 600) {
      return const TabletLayout();
    }
    return const MobileLayout();
  },
)

// ✅ GOOD: MediaQuery 활용
final screenWidth = MediaQuery.of(context).size.width;
final isTablet = screenWidth > 600;
```

## 이미지 처리
```dart
// ✅ GOOD: CachedNetworkImage 사용
CachedNetworkImage(
  imageUrl: trainer.profileUrl,
  placeholder: (context, url) => const CircularProgressIndicator(),
  errorWidget: (context, url, error) => const Icon(Icons.error),
  fit: BoxFit.cover,
)

// ❌ BAD: Image.network 직접 사용
Image.network(trainer.profileUrl)
```

## Theme 일관성
```dart
// core/theme/app_colors.dart
class AppColors {
  static const primary = Color(0xFF6200EE);
  static const secondary = Color(0xFF03DAC6);
  static const error = Color(0xFFB00020);
}

// core/theme/app_text_styles.dart
class AppTextStyles {
  static const heading1 = TextStyle(
    fontSize: 32,
    fontWeight: FontWeight.bold,
  );
}

// core/theme/app_spacing.dart
class AppSpacing {
  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 16.0;
  static const lg = 24.0;
  static const xl = 32.0;
}

// ✅ GOOD: Theme 사용
Text(
  'Hello',
  style: Theme.of(context).textTheme.headlineMedium,
)

// ❌ BAD: 하드코딩
Text(
  'Hello',
  style: TextStyle(fontSize: 24, color: Colors.blue),
)
```

## Asset 관리
```dart
// core/constants/app_assets.dart
class AppAssets {
  static const logo = 'assets/images/logo.png';
  static const placeholder = 'assets/images/placeholder.png';
  static const iconDumbbell = 'assets/icons/dumbbell.svg';
}

// ✅ GOOD
Image.asset(AppAssets.logo)

// ❌ BAD
Image.asset('assets/images/logo.png')
```

## 접근성
- 터치 타겟은 최소 `48x48dp` 보장
- 대비율은 WCAG AA 기준 (4.5:1) 준수
- `Semantics` 위젯으로 스크린 리더 지원
