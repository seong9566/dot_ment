import 'package:flutter/material.dart';

/// 앱 색상 상수
class AppColors {
  AppColors._();

  // Primary Colors (Purple Gradient)
  /// Primary 메인 색상 (8800BB)
  static const primary = Color(0xFF8800BB);
  
  /// Primary 100 (AA33CC)
  static const primary100 = Color(0xFFAA33CC);
  
  /// Primary 200 (BB66DD)
  static const primary200 = Color(0xFFBB66DD);
  
  /// Primary 300 (CC80E5)
  static const primary300 = Color(0xFFCC80E5);
  
  /// Primary 400 (D9A1F2)
  static const primary400 = Color(0xFFD9A1F2);
  
  /// Primary 500 (E6C2FF)
  static const primary500 = Color(0xFFE6C2FF);
  
  /// Primary 600 (F2D9FF) - 가장 밝은 톤
  static const primary600 = Color(0xFFF2D9FF);

  // Text Colors
  /// 메인 텍스트 색상 (4A4A4A)
  static const textPrimary = Color(0xFF4A4A4A);
  
  /// 보조 텍스트 색상
  static const textSecondary = Color(0xFF757575);
  
  /// 비활성 텍스트 색상
  static const textDisabled = Color(0xFFBDBDBD);

  // Semantic Colors
  /// 에러 색상 (FF4F4F)
  static const error = Color(0xFFFF4F4F);
  
  /// 성공 색상
  static const success = Color(0xFF4CAF50);
  
  /// 경고 색상
  static const warning = Color(0xFFFF9800);
  
  /// 정보 색상
  static const info = Color(0xFF2196F3);

  // Neutral Colors
  /// 배경 색상
  static const background = Color(0xFFFFFFFF);
  
  /// 서페이스 색상
  static const surface = Color(0xFFFFFFFF);
  
  /// 구분선 색상
  static const divider = Color(0xFFE0E0E0);
  
  /// 테두리 색상
  static const border = Color(0xFFE0E0E0);
}
